spring:
  datasource:
    username: root
    password: maksi
    url: jdbc:postgresql://localhost:5434/scholarships
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: none
  flyway:
    enabled: true
    locations: classpath:db/migration

server:
  port: 8110

logging:
  level:
    org.flywaydb: DEBUG
